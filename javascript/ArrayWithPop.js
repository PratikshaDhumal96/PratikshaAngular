// we can remove an item from an array with pop()

var OurArray=[1,2,3];
var RemovedFromOurArray=OurArray.pop();

//RemovedFromOurArray is now equals 3,and ourArray now equals[1,2]

console.log(OurArray);


var MyArray=[3,4,5];
var RemovedFromMyArray=MyArray.pop();
 console.log(MyArray);